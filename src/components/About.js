var React = require('react');
var PropTypes = React.PropTypes;

var About = React.createClass({

  render: function() {
    return (
      <div>
        About
      </div>
    );
  }

});

module.exports = About;
