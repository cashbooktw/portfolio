var React = require('react');
var PropTypes = React.PropTypes;

var MultiplayerGame = React.createClass({

  render: function() {
    return (
      <div>MultiplayerGame</div>
    );
  }

});

module.exports = MultiplayerGame;
